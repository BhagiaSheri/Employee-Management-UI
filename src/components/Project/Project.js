function Project(props) {

    return (
        <div className="Project">
            {"Id: " + props.id + " - Project Name: " + props.name}
            <div>
            </div>
        </div>
    )
}

export default Project;